
```
SELECT <columnNames>
FROM <table>
GROUP BY <columnNames2>;
```

```
SELECT <columnNames>, COUNT(<columnName>)
FROM <table>
GROUP BY <columnNames>;
```

```
SELECT <columnNames>
FROM <table>
GROUP BY <columnNames>
ORDER BY <columnNames2>;
```


```

```